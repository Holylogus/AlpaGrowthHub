export default class PublicView {
    constructor(){
        //htmlConstruct();
    }

    htmlConstruct(){
        for (let index = 0; index < array.length; index++) {
            const element = array[index];
            
        }
    }
}